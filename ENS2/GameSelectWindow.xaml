<Window x:Class="ENS2.GameSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ENS2"
        mc:Ignorable="d"
        Title="Select game" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen" ResizeMode="CanResize" >
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0">Выберите игру:</TextBlock>

        <TextBox Name="Password" Grid.Row="1" Grid.Column="0" >спсиок игр</TextBox>

        <WrapPanel Grid.Row="2" Grid.Column="2" HorizontalAlignment="Right" Margin="5,5,5,5">
            <Button IsDefault="True" MinWidth="60" Margin="5,5,5,5">OK</Button>
            <Button IsCancel="True" MinWidth="60" Margin="5,5,5,5">Отмена</Button>
        </WrapPanel>
    </Grid>
</Window>
